Day 2 Summary

- We first tried to collect the dataset from BigQuery.
- The BigQuery export path had billing/payment issues, so the data could not be taken that way.
- Later, we successfully extracted the data from Kaggle in CSV format.
- The main tables saved locally are users, orders, products, and order_items.
- The remaining tables can be used later if needed, but for now the important data is available locally.
- Day 2 is complete because the project now has the required data files for local development.